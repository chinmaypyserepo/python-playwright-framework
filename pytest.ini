[pytest]
testpaths = tests
addopts = -ra --strict-markers -m "not intentional_failure"
markers =
    ui: browser-based end-to-end tests
    api: API contract and workflow tests
    smoke: short critical-path tests
    intentional_failure: opt-in test used to verify failure reporting
